<div id='topbar'>
  <div id='topbar-wrap'>
    <% if !current_user.nil? %>
      <ul class='logged'>
        <li>Logged in as: <%= current_user.username %></li>
        <li><%= button_to "Logout", session_url, :method => :delete %></li>
      </ul>
    <% end %>

    <p class='logo'>Music Pins</p>

    <a class='add-pin' href='#'>Add a pin</a>
  </div>
</div>

<div id='content'>
</div>

<script>

$(document).ready(function(){
  MP.initialize();
});
</script>